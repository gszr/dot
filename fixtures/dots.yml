files:
  gitconfig:
    dst: out/gitconfig
    os: all # all, linux, macos
  zshrc:
    dst: out/zshrc
    typ: copy
    os: linux
    opt: # copy opts NYI
files_opts:
  dir: fixtures/

# handle directories
